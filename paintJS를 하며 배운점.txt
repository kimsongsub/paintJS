* VScode에서 태그명.클래스이름 -> 엔터 = 바로 생성
------------------------------------------------------------------------------------
- 보통 HTML에서 JS 작업에는 id(#)를 끌어와서 쓰고 CSS(.) 작업에는 클래스를 끌어와서 씀
- 각각이 너무 다 다르면 HTML에서 style 태그 등으로 바로 적용 시킴
------------------------------------------------------------------------------------
- git commit : 로컬에 현재 버전을 저장 (일반 저장과는 다르게 버전 관리라 문제 발생시 이전 버전으로 recover가 가능함)
- push : github에 업로드
